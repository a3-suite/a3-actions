version: 1
name: contract-clauses
subject_ref: subject.ci.jq-provisioner
contracts:
  - id: contract.ci-jq-provisioner.provision
    summary: A checked official jq Release asset enters the job PATH; failed provisioning exposes no executable.
    conditions:
      - id: jq-release-valid
        environment_predicate:
          kind: environment-ref
          target_ref: runtime-context.jq-release-asset-valid
          op: available
      - id: jq-release-invalid
        environment_predicate:
          kind: environment-ref
          target_ref: runtime-context.jq-release-asset-invalid
          op: available
    guarantees:
      - id: checked-jq-available
        when: jq-release-valid
        observation_ref: observation.ci-jq-provisioner.path
        claim:
          kind: observable-result
          op: present
          target:
            kind: observation_field
            value: jq
      - id: failed-provisioning-stops
        when: jq-release-invalid
        observation_ref: observation.ci-jq-provisioner.result
        claim:
          kind: observable-value
          op: in
          relation:
            left:
              kind: observation_field
              value: outcome
            right:
              kind: value_set
              values:
                - failure
    verification_intents:
      - id: checked-jq-provisioned
        goal: A checksum-matched Release asset is available to later job steps.
        guarantee_refs:
          - checked-jq-available
      - id: failed-jq-provisioning-rejected
        goal: Missing or invalid Release assets stop provisioning before PATH handoff.
        guarantee_refs:
          - failed-provisioning-stops
