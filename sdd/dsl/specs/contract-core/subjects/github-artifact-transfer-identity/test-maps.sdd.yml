version: 1
name: contract-test-maps
subject_ref: subject.ci.github-artifact-transfer-identity
test_maps:
  - verification_id: verification.ci.github-artifact-transfer-identity.outputs
    case_id: matching-provider-identity
    tests:
      - path: actions/github-artifact-transfer-identity/tests/entrypoint.test.ts
        symbol: bundled entrypoint verifies a mocked GitHub API response
        check_refs: [check-artifact-identity-output]
