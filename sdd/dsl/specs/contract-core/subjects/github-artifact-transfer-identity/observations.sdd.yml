version: 1
name: contract-observations
subject_ref: subject.ci.github-artifact-transfer-identity
observations:
  - id: observation.ci.github-artifact-transfer-identity.outputs
    summary: The Action exposes the provider identity after all fields match.
    boundary: {ref: boundary.action-output}
    value:
      root: action.outputs
      kind: object
      fields:
        - {selector: status, kind: observation_field, meaning: Verification status.}
        - {selector: artifact-transfer-id, kind: observation_field, meaning: Verified artifact ID.}
        - {selector: artifact-transfer-digest, kind: observation_field, meaning: Verified normalized digest.}
